{
    "category": "threestudio",
    "question": "Please use FileSystem tools to finish the following task:\n\n### Task Description\n\nThe `requirements.txt` file in the ThreeStudio project is used to install necessary Python libraries. However, the Zero123-related dependencies were accidentally deleted from the file. Your task is to restore these missing dependencies.\n\n### Task Objectives\n\n1. **Locate the requirements.txt file** in the test environment\n2. **Identify the missing Zero123 dependencies** that need to be restored\n3. **Add the required dependencies** to the requirements.txt file\n4. **Ensure the file format is correct** (one dependency per line)\n\n### Required Dependencies to Restore\n\nThe following dependencies must be present in requirements.txt:\n- einops\n- kornia\n- taming (taming-transformers-rom1504)\n- openai and clip should be on the same line (openai-clip)\n\n### Expected Output\n\nThe `requirements.txt` file should:\n- Contain all five required dependencies (einops, kornia, taming, openai, clip)\n- Have openai and clip on the same line\n- Be properly formatted\n- Be non-empty and valid\n\n### Success Criteria\n\n- requirements.txt file exists and is readable\n- All required dependencies are present\n- File format is valid",
    "use_specified_server": true,
    "mcp_servers": [
        {
            "name": "filesystem_mcpmark"
        }
    ],
    "evaluators": [
        {
            "func": "raw",
            "op": "mcpmark.filesystem.threestudio.verify_requirements_completion",
            "value": "completed",
            "op_args": {},
            "desc": "Verify Zero123 dependencies correctly restored in requirements.txt"
        }
    ],
    "prepares": [
        {
            "prepare_func": "mcpmark_filesystem_setup",
            "prepare_args": {}
        }
    ],
    "cleanups": [
        {
            "server": "mcpmark",
            "tool": "",
            "cleanup_func": "filesystem_cleanup",
            "cleanup_args": {}
        }
    ]
}
